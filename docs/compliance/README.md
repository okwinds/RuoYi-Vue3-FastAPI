# Compliance Docs（合规文档）

本目录用于沉淀“与交付合规相关”的约定，目标是：
- 让 fork 的开源项目在持续改造过程中仍然可同步、可审计、可复现；
- 降低“人走知识丢失/环境不可复现/回归不可定位”的工程风险。

当前包含：
- `fork-upstream.md`：上游同步与改造边界约定

建议后续逐步补齐（按需）：
- 依赖与许可证清单（含生成方式）
- 安全基线（密钥处理、默认账号、环境隔离）
- 数据库迁移与回滚策略（alembic / SQL 初始化脚本）

